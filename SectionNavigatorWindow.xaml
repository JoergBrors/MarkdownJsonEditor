<Window x:Class="MarkdownJsonEditor.SectionNavigatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Section" Height="400" Width="500" 
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Multiple sections found. Please select one to edit:" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" 
                 Name="SectionListBox" 
                 FontSize="14"
                 Margin="0,0,0,10"
                 MouseDoubleClick="SectionListBox_MouseDoubleClick">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="10,5"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <TextBlock Grid.Row="2" 
                   Text="Tip: Double-click on a section to edit it, or use 'Combine All' to merge everything." 
                   FontStyle="Italic" 
                   FontSize="11"
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Name="EditButton" 
                    Content="Edit Selected" 
                    Width="120" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="EditButton_Click"
                    IsDefault="True"/>
            <Button Name="CombineButton" 
                    Content="Combine All" 
                    Width="120" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="CombineButton_Click"/>
            <Button Name="CancelButton" 
                    Content="Cancel" 
                    Width="80" 
                    Height="30" 
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
